% Section 1.4: 研究目标与主要贡献

本文以“面向算法代码生成的大语言模型指令微调与推理增强”为主题，围绕1.3所归纳的核心问题，设定如下总体研究目标：

\begin{enumerate}
  \item 构建算法代码生成任务下训练–评测一体化的标准化实验体系，确保不同方法在数据、提示与评测维度具备严格可比性；
  \item 在有限算力约束下，利用参数高效微调技术，实现基座模型在算法题上可执行正确性与生成稳定性的量化提升；
  \item 将\textbf{训练侧}（是否微调、是否 CoT 监督、是否注入少样本）与\textbf{推理侧}（Direct / Zero-shot CoT / Few-shot CoT）界定为两个独立因素，在统一协议下量化其主效应、交互效应及少样本示例的边际贡献；
  \item 建立从数据处理、训练到评测的端到端可复现实验框架，将所得方法层面的实证结论沉淀为可直接复用的实验资产，为算法代码生成系统的后续迭代与扩展研究提供依据。
\end{enumerate}

围绕上述目标，本文的主要工作与贡献可以概括为以下四点：

\subsection{标准化、可复现的端到端实验框架}
本文构建了覆盖数据处理、提示构造、模型推理、可执行性评测的完整实验流程，实现训练与评测环节的统一规范。在统一协议下对基座模型、高效微调、零样本推理、少样本推理等方案开展对照，为方法对比提供了公平、可靠的实验基础。

\subsection{参数高效微调在算法代码生成中的有效落地}
面向算法题任务特点，设计适配的微调样本构造、训练目标与参数配置策略，在不显著增加推理开销的前提下，显著提升代码执行通过率与生成稳定性。实证结果验证了参数高效微调在算法代码生成场景下的有效性与工程实用性。

\subsection{训练 \texttimes{} 推理两因素析因的系统对照与机理分析}
本文将训练侧与推理侧视为两个独立因素进行组合对照，具体因素水平如下：
\begin{itemize}
    \item \textbf{训练侧（4 水平）}：Base、纯代码 LoRA（\texttt{LoRA\_code}）、零样本 CoT 格式 LoRA（\texttt{LoRA\_cot\_zs}）、少样本 CoT 格式 LoRA（\texttt{LoRA\_cot\_fs}）；
    \item \textbf{推理侧（3 水平）}：Direct、Zero-shot CoT、Few-shot CoT；
    \item \textbf{合法性约束}：若训练侧与推理侧均含少样本示例，则两侧示例数 $k$ 必须一致，由此确定 $H_1$--$H_{10}$ 共 $10$ 组有效配置（完整矩阵见 4.1 节）。
\end{itemize}
基于该析因框架，本文在统一评测协议下估计训练侧主效应、推理侧主效应及其交互项，并量化少样本边际效应在不同训练侧上的一致性，形成“何时应该微调、何时应该 CoT、何时应该注入示例”的可操作判据。

\subsection{可复现实验支撑}
本文将整套数据流水线、训练与评测脚本、提示模板及配置口径以仓库内绝对路径形式固定，使从原始 KodCode 数据到 $H_1$--$H_{10}$ 全部 pass@$k$ 结果可一键重跑；同时对训练-评测一致性约束、合法性约束、随机性来源与超参口径进行统一记录。该框架既保证了本文实证结论的可验证性，也为后续在更大数据规模、更多基座模型、更细粒度失效模式归因等方向上的扩展研究提供了直接可用的实验底座。